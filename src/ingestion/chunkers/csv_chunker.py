class CSVChunker:

    def chunk_documents(self, documents):

        print(
            f"✅ CSV documents already "
            f"represent semantic chunks: "
            f"{len(documents)} chunks"
        )

        return documents